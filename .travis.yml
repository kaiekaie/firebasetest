language: node_js
node_js:
  - "7"
branches:
  only:
    - master

before_script:
  - npm install -g firebase-tools

after_success:
   - cd functions && npm install
   - firebase deploy --token $FIREBASE_TOKEN
